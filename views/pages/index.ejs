<%- include('../partials/header') -%>
<h1>
  HS Lead Notify for Slack
</h1>
	<% if(!user){ %>
		<a href="/login">Login</a>
    <a href="/register">Register</a>
	<% } else {  %>
   <p> You're logged in as <%= user.username %> </p>
   <% } %>

<%- include('../partials/footer') -%>
